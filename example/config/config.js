'use strict';

const config = {
  server: {
    host: 'localhost',
    port: '3000',
    banner: 'Mongoose CRUD for RESTful Server'
  },
  serverURL: 'http://localhost:3000',
  mongo: {
    dbname: 'mongoose-restful-crud',
    host: 'localhost:27017'
  },
};

module.exports = config;
